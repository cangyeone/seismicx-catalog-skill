# Data Contracts

Use these contracts when normalizing inputs and exchanging data between pickers, associators, locators, magnitude code, and focal-mechanism code.

## Waveforms

- Accept MSEED, miniSEED, SAC, SEED, SEG-Y, GSE, GCF, and other formats supported by `obspy.read`.
- Preserve the original file path in all downstream pick rows as `waveform_path`.
- Prefer one trace id format everywhere: `NET.STA.LOC.CHA`. Empty location codes may be represented as an empty field in CSV and omitted only in display text.
- Never assume a directory naming convention. Always scan headers first with:
  `python scripts/seismicx_catalog.py scan -w <waveforms> -o work/waveforms.csv`

## Station Metadata

Canonical station CSV columns:

```csv
station_id,network,station,location,longitude,latitude,elevation_m
YN.C2714.40,YN,C2714,40,105.1287,27.6082,1612.0
```

Legacy whitespace station files are also accepted:

```text
YN C2714 40 105.1287 27.6082 1612.0
```

For ML, station response is required for calibrated amplitudes. Use StationXML where possible. If only RESP/dataless files exist, convert them before the ML step or explicitly mark ML as uncalibrated/raw-scaled.

The helper also accepts a seedtools-style `sta.resp.path` mapping file whose values point to RESP/StationXML files.

## Velocity Model

Use a simple CSV or whitespace model for baseline location and for configuring external engines:

```csv
depth_km,vp_km_s,vs_km_s
0,5.8,3.35
10,6.2,3.58
20,6.8,3.92
```

Always confirm whether depths are positive downward and velocities are km/s. For `bayes_location`, pass the model in the format required by the local adapter and record the adapter command.

## Picks

Canonical pick CSV columns:

```csv
pick_id,event_id,waveform_path,trace_id,network,station,location,channel,phase,time,score,snr,amplitude,polarity,polarity_quality,polarity_score,picker
p00000001,,/data/day001/YN.C2714.40.EIZ.mseed,YN.C2714.40.3C,YN,C2714,40,3C,Pg,2023-05-03T04:08:29.140000Z,0.58,2.23,80.1,U,E,2.4,torchscript-pnsn:pnsn.v3.jit
```

Rules:

- `time` must be UTC ISO text.
- `phase` may be generic (`P`, `S`) or regional (`Pg`, `Sg`, `Pn`, `Sn`). Keep the user's selected phase labels when possible.
- `event_id` is empty before association and filled after association.
- `polarity` is `U`, `D`, or `N`; `polarity_quality` is `I`, `E`, or empty.
- `waveform_path` may contain semicolon-separated component files for a three-component picker; downstream helper commands read those as one logical stream.

## Events

Canonical event CSV columns:

```csv
event_id,origin_time,latitude,longitude,depth_km,magnitude,magnitude_type,rms,n_picks,method
ev000001,2023-05-03T04:08:19.120000Z,28.11,105.14,10.0,4.1,ML,0.21,18,grid-least-squares
```

`events.csv` may be generated by GaMMA, REAL, the grid locator, or `bayes_location`. If an external tool emits a different schema, convert it to the canonical columns before ML, analysis, plotting, or focal-mechanism calculation.

## Station Magnitudes

Station ML output follows the seedtools/DD1 convention:

```csv
event_id,station_id,max_amp_um,max_amp_e_um,max_amp_n_um,period_e_s,period_n_s,time_sme,time_smn,distance_km,ml,method,quality
ev000001,YN.C2714.40,125.4,118.2,132.6,0.42,0.45,2023-05-03T04:08:33.120000Z,2023-05-03T04:08:33.180000Z,32.4,2.31,seedtools-dd1-R13,seedtools_response_simulated_um
```

Rules:

- `max_amp_um` is the E/N average when both horizontal components are valid; otherwise it is the valid component amplitude.
- `max_amp_e_um` and `max_amp_n_um` are half peak-to-trough horizontal amplitudes after response simulation, in micrometers.
- `period_e_s` and `period_n_s` are measured around the selected maximum-amplitude cycle.
- `method` records the regional R curve, for example `seedtools-dd1-R13`.
- `quality=raw_scaled_um` means no response metadata was used and the result is preliminary only.

## Focal Mechanisms

Mechanism CSV columns:

```csv
event_id,mechanism_strike,mechanism_dip,mechanism_rake,mechanism_aux_strike,mechanism_aux_dip,mechanism_aux_rake,mechanism_n_polarities,mechanism_misfit_fraction,mechanism_quality,mechanism_method
ev000001,230.0,60.0,-90.0,50.0,30.0,-90.0,12,0.167,B,first-motion-grid
```

Rules:

- `mechanism_*` columns can be merged directly into the final catalog.
- `mechanism_quality=insufficient` means the event did not have enough reliable P first motions.
- Use `mechanism_method=first-motion-grid` for the built-in baseline and preserve the HASH output separately when a HASH/pyhash solution is used.

## Suggested Workspace

```text
work/
  waveforms.csv
  waveform_errors.csv
  picks.csv
  picks_associated.csv
  picks_with_polarity.csv
  events_associated.csv
  assignments.csv
  events_located.csv
  events_ml.csv
  station_ml.csv
  mechanisms.csv
  catalog_final.csv
  hash_input.csv
  activity.json
  daily_counts.csv
  catalog_map.png
  build_manifest.json
```
